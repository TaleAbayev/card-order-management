server:
  port: ${API_SERVER_PORT:8081}
  servlet:
    application-display-name: card-order

info:
  app:
    name: "${project.name}"
    description: "${project.description}"
    version: "${project.version}"
    java:
      version: "${java.version}"